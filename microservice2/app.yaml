apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: micro2
  namespace: default
spec:
  project: default
  source:
    repoURL: https://github.com/deepakjotwani/argo-poc.git 
    targetRevision: HEAD
    path: microservice2/micro2
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: false
      selfHeal: false
