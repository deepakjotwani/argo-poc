apiVersion: apps/v1
kind: Deployment
metadata:
  name: micro1
spec:
  replicas: 1
  selector:
    matchLabels:
      app:  micro1
  template:
    metadata:
      labels:
        app:  micro1
    spec:
      containers:
      - image: micro1-service
        name:  micro1
        ports:
        - containerPort: 8080
        imagePullPolicy: IfNotPresent
---

  apiVersion: v1
  kind: Service
  metadata:
    name: deepakjotwani/sample2
    annotations:
  spec:
    ports:
    - port: 8080
      targetPort: 8080
    selector:
      app:  micro1
    type: LoadBalancer
    
